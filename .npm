#!/bin/bash

packages="eslint grunt-cli a11y dark-mode trash http-server"

echo "Installing NPM Packages..."

# Install packages globally and quietly
npm install $packages --global --quiet

[[ $? ]] && echo "Done"
